import React from 'react';

function KittenPrompt(kittenName, birthMonth, birthDay, birthYear, currentWeight) {
    var kittenName = prompt("Enter the kitten's name please");
    var birthMonth = prompt("enter the two digits of the birth month");
    var birthDay = prompt("enter the two digits of the birth day");
    var birthYear = prompt("enter the four digits of the birth year");
    var currentWeight = prompt("enter kitten's weight in lbs");
  
    return this.newKitten.kittenName;
}

export default KittenPrompt